export const carsModels = ['BMW', 'Audi', 'Volvo', 'Mercedes-Benz'];
const BIRTHDAY_DATE = '29.01.2001';

export { BIRTHDAY_DATE };

const sayHi = (greet) => {
  return `${greet} dear user!`;
};

const sayBye = (greet) => {
  return `${greet} dear user!`;
};

export { sayBye, sayHi };
